from setuptools import setup

setup(
    name='npest',
    version='0.1.0',
    py_modules=['npest'], # This tells pip to ONLY install the npest.py file
    description='Non-parametric estimation tools',
)